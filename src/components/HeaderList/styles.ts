import styled from 'styled-components';

export const Header = styled.header`
  margin-top: 4rem;
  padding-left: 8px;
  padding-right: 8px;
  display: flex;
  flex-direction: row;

  .title {
    margin-left: 6rem;
    font-size: x-large;
  }
`;
